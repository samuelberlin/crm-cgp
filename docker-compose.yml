services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
      POSTGRES_DB: crm_cgp
    ports:
      - "5432:5432"
    volumes:
      - crm_cgp_db:/var/lib/postgresql/data

volumes:
  crm_cgp_db:
